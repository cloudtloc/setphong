name: diemdanh_face
description: Ung dung diem danh khuon mat (Flutter).
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  camera: ^0.11.4
  flutter_image_compress: ^2.0.0
  geolocator: ^13.0.4
  google_maps_flutter: ^2.15.0
  google_mlkit_face_mesh_detection: ^0.4.2
  image: ^4.0.0
  image_picker: ^1.0.0
  permission_handler: ^12.0.0
  http: ^1.0.0
  flutter_dotenv: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - env.template
